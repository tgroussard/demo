<nav class="navbar navbar-expand-lg navbar-dark bg-dark text-light p-0">
    <a class="navbar-brand p-0 m-0" href="{{ path('app_home') }}"><img style="width: 150px" src="{{ asset('img/brand.png') }}" alt=""></a>
    <div class="mr-auto" style="width: 50px; height: 60px; background: linear-gradient(to right, #1d3458, #343a40)"></div>
    <button class="navbar-toggler m-2" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse p-2" id="navbarText">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('app_home') }}">Accueil <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_profil') }}">Mon profil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter ({{ app.user.pseudo }})</a>
            </li>
        </ul>
    </div>
</nav>